[app]
name         = "geckoterminal"                  # slug — becomes the release tag
display_name = "GeckoTerminal Pools"            # human-readable
platform     = "krexa"
git          = "https://github.com/aomi-labs/krexa-hosted-apps"
public       = true

# Built by aomi-smither (Smithers-orchestrated: codegen from the GeckoTerminal
# OpenAPI spec, Claude-curated tool surface, cargo-validated, live-smoked).
# Defaults to server_tags = ["staging"].
